server_tokens off;
server{
        listen 80 default_server;
        client_max_body_size 20M;
        root /html;
        index index.html index.htm;
        location / {
                try_files $uri /index.html;
        }
}
