name: juegos_react

services:

    juegos_react:
        container_name: juegos_react
        image: juegos_react_imagen
        build: 
            context: ./
            dockerfile: ./Dockerfile
        tty: true
        command:
            - /bin/sh
            - -c
            - |
                mv -f /tmp/yarn.lock /nodebuild/
                rm -R /nodebuild/node_modules
                mv -f /tmp/node_modules /nodebuild/
                nginx -g 'daemon off;'
        volumes:
            - ./nodebuild:/nodebuild
            - /etc/timezone:/etc/timezone:ro
            - /etc/localtime:/etc/localtime:ro
        ports:
            - 80:80
        # restart: unless-stopped
