image: node

pages:
    stage: deploy
    cache:
        paths:
            - node_modules/
    script:
        - npm install
        - npm run build
        - rm -Rf public
        - mv build public
    artifacts:
        paths:
            - public
    only:
        - master
